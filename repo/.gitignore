.DS_Store
*.bak
*.swp
*.tmp

# age / sops приватные ключи — никогда в Git
*keys.txt
*.agekey
*.age.txt
.sops/keys.txt
.sops/*.txt

# secrets/ теперь Helm chart — Chart.yaml, values.yaml, templates/ коммитим.
# Блокируем только plaintext values перед шифрованием: sops читает его из
# values-secret.yaml и отдаёт values-secret.enc.yaml (последний коммитим).
secrets/values-secret.yaml
secrets/values-secret.yml
