replicaCount: 2

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

ingress:
  enabled: false
  host: nginx-prod.local

config:
  indexHtml: |
    <html>
      <body>
        <h1>nginx-demo / prod</h1>
        <p>Rendered by ArgoCD ApplicationSet</p>
      </body>
    </html>
