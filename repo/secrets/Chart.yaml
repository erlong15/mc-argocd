apiVersion: v2
name: secrets
description: Рендерит k8s Secret'ы по списку из values. Чувствительные значения едут через helm-secrets/sops в values-secret.enc.yaml; в Git хранится только ciphertext.
type: application
version: 0.1.0
